# -*- coding: utf-8 -*-
# Core
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.0

# AI / LangChain
langchain>=0.3.0
langchain-openai>=0.3.0
langgraph>=0.2.0
google-genai>=1.0.0

# Auth (BE-02 — JWT + argon2id)
pyjwt>=2.10.0
passlib[argon2]>=1.7.4
email-validator>=2.0.0

# Database (BE-01 — schema thật + migration)
sqlalchemy>=2.0.0
alembic>=1.14.0
# psycopg2-binary bắt buộc khi deploy Postgres thật; SQLite (mặc định dev/test)
# không cần driver rời, dùng thẳng sqlite3 built-in của Python.
psycopg2-binary>=2.9.0

# Vector Store (uncomment as needed)
# chromadb>=0.5.0

# Optimization (AGT-09 — CP-SAT menu optimizer)
ortools>=9.11.0

# Dev tools
ruff>=0.8.0
mypy>=1.13.0
types-passlib>=1.7.7
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.28.0
